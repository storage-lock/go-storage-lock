package server

// TODO
// 启动一个HTTP服务，提供分布式锁的API操作

func RunStorageLockAPIServer(options *StorageLockAPIServerOptions) {
	//e := echo.New()
	//
	//// Middleware
	//e.Use(middleware.Logger())
	//e.Use(middleware.Recover())
	//
	//// Routes
	//e.GET("/lock", hello)
	//
	//// Start server
	//e.Logger.Fatal(e.Start(":1323"))
}
